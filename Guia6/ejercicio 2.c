#include <stdio.h>
#include <stdlib.h>

void prueba(char letra, int i, float j);

void main()
{
    int x;
    float y;
    char z;
}

void prueba(char letra, int i, float j)
{
}

/*Cuales de las siguientes invocaciones desde la funcio main son correctas?
prueba(i,x,z);
prueba(letra, i, j);
prueba;
h=prueba(z,x,y); <- invocacion correcta*/